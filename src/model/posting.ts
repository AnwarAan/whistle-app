const structure = [
  { name: "content", title: "Content", type: "text-area" },
  { name: "image", title: "image", type: "file" },
];

const url = "/post";
const model = { structure, url };
export default model;
